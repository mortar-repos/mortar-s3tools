source 'https://rubygems.org'

gem 'aws-sdk'

# Development only dependencies. Typically used by
# the rake file to automate installation on file change.
#
group :development do
  gem 'rb-inotify', :require => false
  gem 'rb-fsevent', :require => false
  gem 'rb-fchange', :require => false
  gem 'listen'
  gem 'rake'
end
